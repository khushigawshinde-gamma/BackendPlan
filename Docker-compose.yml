version: '3.9'

services:

  db:
    image: postgres:16
    container_name: planners_db

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Post123
      POSTGRES_DB: event_planners

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build: .

    container_name: planners_api

    ports:
      - "8000:8000"

    depends_on:
      - db

    env_file:
      - .env

volumes:
  postgres_data: